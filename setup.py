from distutils.core import setup

setup(name="srec2bin_py",
      description="This utility converts binary data from Motorola S-Record file format to raw binary",
      version="1.0",
      py_modules=["srec2bin",
                  "srecord"])
